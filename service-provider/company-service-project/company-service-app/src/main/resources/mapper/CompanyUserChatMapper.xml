<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.db.chaomaxs.companyservice.dal.mysql.mapper.chat.CompanyUserChatMapper">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.db.chaomaxs.companyservice.dal.mysql.dataobject.chat.CompanyUserChatDO" id="BaseResultMap">
        <result property="id" column="id"/>
        <result property="companyUserId" column="company_user_id"/>
        <result property="userId" column="user_id"/>
        <result property="positionId" column="position_id"/>
        <result property="status" column="status"/>
        <result property="sort" column="sort"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.db.chaomaxs.companyservice.dal.mysql.dataobject.chat.CompanyUserChatStatisticsDO" id="BaseStatisticsResultMap">
        <result property="positionId" column="position_id"/>
        <result property="count" column="count"/>
    </resultMap>
    <select id="getByUserIdAndCompanyUserId" resultMap="BaseResultMap">
        SELECT id,user_id from company_user_chat where company_user_id=#{companyUserId} and company_id=#{companyId} and is_deleted=0
    </select>

    <select id="getStatisticByPositionIds" resultMap="BaseStatisticsResultMap">
        SELECT position_id, count( 1 ) AS count FROM company_user_chat
        WHERE position_id IN
        <foreach item="item" collection="ids" open="(" separator="," close=")">
            #{item}
        </foreach>
        AND is_deleted = 0 GROUP BY position_id
    </select>

</mapper>
