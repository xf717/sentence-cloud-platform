package ${package}.mapper;

import ${package}.client.dto.${className}DTO;
import ${package}.entity.${className}DO;
import com.baomidou.mybatisplus.core.mapper.BaseMapper;

import java.util.List;

/**
 * ${comments}
 *
 * @author ${author}
 * @date ${datetime}
 */
public interface ${className}Mapper extends BaseMapper<${className}DO> {

	/**
	 * 查找数据
	 * @param dto
	 * @return
	 */
	List<${className}DTO> query(${className}DTO dto);

	/**
	 * 插入数据，并返回主键ID
	 * @param dto
	 * @return
	 */
	int insertSelective${className}(${className}DTO dto);

}
